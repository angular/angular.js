'use strict';

/**
 * @ngdoc overview
 * @name ngSensor
 * @description
 *
 * # ngSensor
 *
 * `ngSensor` is the name of the optional Angular module that provides sensor events and other
 * helpers for sensor-enabled devices.
 * The implementation is based on W3C sensor-api spec
 * ([https://w3.org](https://dvcs.w3.org/hg/dap/raw-file/tip/sensor-api/))
 *
 * {@installModule sensor}
 *
 * See {@link ngSensor.$motion `$motion`} for usage.
 */

// define ngSensor module
var ngSensor = angular.module('ngSensor', []);

